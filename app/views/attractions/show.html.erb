<div class="heading"><h1><%= @attraction.name %></h1></div>

<br><br>

<div class="content">
    <h3>A little about our ride:</h3>
    <ul>
        <li>Nausea Rating: <%= @attraction.nausea_rating %></li>
        <li>Satisfaction Rating: <%= @attraction.happiness_rating %></li>
        <li>Ticket Requirement: <%= @attraction.tickets %></li>
        <li>Minimum Height Requirement (in inches): <%= @attraction.min_height %></li>
    </ul>
    
    <br><br>
    
    <% if !current_user.admin %>
    <form action="/rides/new" method="post"> 
        <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
        <input name="user_id" type="hidden" value="<%= @user.id %>"> <input name="attraction_id" type="hidden" value="<%= @attraction.id %>">  
        <input type="submit" value="Go on this ride">
    </form>
    <br><br>
    <% else %>
    <button class="btn btn-primary"><%= link_to "Edit Attraction", edit_attraction_path %></button>
    <% end %>
    
</div>